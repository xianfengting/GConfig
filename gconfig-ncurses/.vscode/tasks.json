{
    "tasks": [
      {
        "type": "shell",
        "label": "build gconfig-ncurses",
        "command": "/usr/bin/make",
        /*"args": [
          "-g",
          "${file}",
          "-o",
          "${fileDirname}/${fileBasenameNoExtension}",
            "-std=c++11",
            "-lpthread"
        ],*/
        "args": [
          "PROGRAM_NAME=program",
          "DEBUG=1",
          //"ENABLE_SECOND_LEVEL_CODE=1",
          "-w" // 或者 --print-directory
        ],
        "options": {
          "cwd": "${workspaceFolder}/gconfig-ncurses"
        }
      }
    ],
    "version": "2.0.0"
}
